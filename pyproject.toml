[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: marks tests that run real quantum simulators (slow)",
]

[tool.ruff]
line-length = 100
target-version = "py311"
